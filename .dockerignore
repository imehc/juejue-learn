# .dockerignore 里 # 只在行首才是注释，写在模式后面会被当成模式的一部分
#
# 这份文件同时管两个构建：
#   - 前端：context 是仓库根（见 deploy/docker-compose.yml），生效的是这份
#   - 后端：context 是它自己的目录，生效的是
#     meeting_room_booking_system_backend/.dockerignore
.git/
.gitignore
.dockerignore

# 容器内重新安装
**/node_modules

# 已有的构建产物
**/.next
**/dist

# openapi-generator 生成的 API 客户端。必须排除：前端 Dockerfile 先从
# openapi-generate-stage 拷入新生成的客户端，紧接着的
# `COPY meeting_room_booking_system_frontend .` 会用宿主机这份把它覆盖掉，
# 于是生成阶段白跑、实际用的是可能已经过期的本地副本。
# 该目录本身也在前端 .gitignore 里，属于纯生成产物。
**/meeting-room-booking-api

# 部署配置：compose 文件和 nginx 配置由宿主机挂载，不进构建上下文
deploy/

# 测试与运行时产物
**/coverage
**/daily-log
**/*.log

# 编辑器与系统文件
**/.vscode
**/.idea
.DS_Store
**/.DS_Store

# 文档与仓库级工具，构建用不到
*.md
Makefile
.github/
