# ─────────────────────────────────────────────────────────────
# 容器基础设施凭据（开发环境和生产环境共用同一份）
#
# Docker Compose 会自动读取与 compose 文件同目录的 .env，用于：
#   1. 解析 compose 文件里的 ${VAR} 变量
#   2. 通过 env_file 注入到 mysql 容器
#
# 用法：在本目录复制为 .env 后填入真实值。本文件是模板，会提交到
#      仓库；deploy/.env 已被 .gitignore 忽略，不要提交。
#
#   cd deploy && cp .env.example .env
#
# 注意：MySQL 只在数据卷为空时初始化。若卷里已有数据，改这里的
#      密码不会生效，需先 docker compose down -v 清卷再重建。
# ─────────────────────────────────────────────────────────────

# ── MySQL ──
MYSQL_DATABASE=meeting_room_booking_system
# root 密码，建议 16 位以上随机串：openssl rand -base64 24
MYSQL_ROOT_PASSWORD=replace-with-a-strong-password

# ── RustFS（S3 兼容对象存储，替代已归档的 MinIO）──
# Access Key，至少 5 位；不要沿用官方示例的 rustfsadmin
RUSTFS_ACCESS_KEY=replace-me
# Secret Key，至少 8 位：openssl rand -base64 24
RUSTFS_SECRET_KEY=replace-with-a-strong-secret
# 后端上传文件用的桶名，需与后端 yaml 配置里的
# oss-server.bucket-name 一致
RUSTFS_BUCKET=meeting-room-booking-system
